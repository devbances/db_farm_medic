<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dbfarmmedic">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.RegistrarActivity"></activity>
        <activity android:name=".login.LogeoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MedicsClasification.MedicsClasification"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity
            android:name=".Medics.MedicsActivity"
            android:label="@+id/nombre_grupo_medics"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MedicsDetail.MedicsDetailActivity"
            android:label="@string/med"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>